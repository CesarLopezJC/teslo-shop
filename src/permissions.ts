import { Session } from "next-auth";
import { NextURL } from "next/dist/server/web/next-url";


export const permissions = (auth: Session, nextUrl: NextURL) => {
    let isPermited = true;

    //FIXME:

}